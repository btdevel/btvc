Issues:
* When text overwrites menu (e.g. in Guild), menu should reappear after some time
* There needs to be a differnce between e.g. "forward" from programs and from user input
* Maybe we need a user input queue and decide later, how to translate
 

Nice to have:
* 